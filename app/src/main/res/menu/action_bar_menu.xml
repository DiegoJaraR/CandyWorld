<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/add_item"
        android:icon="@android:drawable/ic_menu_add"
        android:title="Agregar Item"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/delete_item"
        android:icon="@android:drawable/ic_menu_delete"
        android:title="Eliminar Item"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/switch_gridview"
        android:icon="@drawable/ic_action_grid"
        android:title="Cambiar a GridView"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/switch_listview"
        android:icon="@drawable/ic_action_list"
        android:title="Cambiar a ListView"
        app:showAsAction="ifRoom" />

</menu>